# Contributing

1. Branch from `develop`: `feat/<module>-<short-name>`, `fix/...`, `chore/...`
2. Conventional commits: `feat(orders): ...`, `fix(api): ...`
3. Run `pnpm lint && pnpm typecheck && pnpm test` before pushing.
4. Architecture changes require an ADR in `docs/decisions/`.
