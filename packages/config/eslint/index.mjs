// Shared ESLint flat config (TypeScript strict).
// TODO: add typescript-eslint, import/no-cycle (circular deps), boundaries rules:
//  - frontend apps may not import from apps/api
//  - modules/* may import other modules only via their index.ts
export default [];
