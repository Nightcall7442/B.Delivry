/**
 * Skeleton props.
 *
 */
import type { HTMLAttributes } from 'react';

export interface SkeletonProps extends HTMLAttributes<HTMLDivElement> {
  /** Round it off for avatars and icon slots. */
  circle?: boolean;
}
