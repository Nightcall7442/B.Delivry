# @bazar/web

Customer web storefront — Next.js App Router. Port 3000.

`src/app` — routes · `src/features` — feature slices · `src/components` — app-level shared UI (design system lives in `@bazar/ui`).
