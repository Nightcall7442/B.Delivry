# @bazar/courier

Bazar Courier — Expo + Expo Router. Routes live in `src/app`, feature slices in `src/features`.

Note: this app uses `node-linker=hoisted` (see `.npmrc`) because React Native/Metro require a flat `node_modules`.
