# pnpm settings
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
# Expo / React Native need hoisted node_modules — override in apps/courier & apps/customer .npmrc if required
