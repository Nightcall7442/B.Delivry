-- Enabled on first container start (dev only)
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";
-- TODO: CREATE EXTENSION IF NOT EXISTS postgis; (image with PostGIS required)
